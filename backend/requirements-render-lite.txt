# Render-optimized requirements.txt for free tier (512MB memory limit)
# This version uses lighter alternatives to reduce memory usage

# Core FastAPI dependencies
fastapi==0.118.0
uvicorn[standard]==0.37.0
pydantic==2.11.9
python-dotenv==1.1.1

# HTTP and requests
requests==2.32.5
httpx==0.28.1

# Lightweight AI/ML dependencies
# Using CPU-only PyTorch to reduce memory
torch>=2.5.0,<3.0.0 --index-url https://download.pytorch.org/whl/cpu
transformers>=4.30.0,<5.0.0
# Use a lighter sentence transformer model
sentence-transformers>=2.2.0,<6.0.0
scikit-learn>=1.3.0,<2.0.0
numpy>=1.24.0,<2.0.0

# Text processing (lightweight alternatives)
textblob>=0.17.0
vaderSentiment>=3.3.0
sumy>=0.11.0

# OpenAI integration
openai>=1.0.0,<3.0.0

# Additional utilities
tqdm>=4.65.0
